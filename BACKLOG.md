# BACKLOG - secfinding
src/location.rs:172 | bug | high | validate_location_file uses std::path::Path::components to detect parent directory traversals, which fails to detect Windows-style backslashes on Unix-like systems | FIXED: normalize '\\'->'/' into a `normalized` binding before Path::new so `..\..\etc\passwd` collapses to ParentDir components and is rejected. Proving test tests/break_it.rs::test_location_backslash_path_traversal (green). | status=done
tests/adversarial/*.rs (location.rs, test_location.rs, test_finding.rs, test_bridge.rs, test_filter.rs, test_reportable.rs, test_evidence.rs, test_kind.rs, test_status.rs) | test-gap | high | Entire tests/adversarial/ subtree is ORPHANED: tests/adversarial.rs only declares `mod adversarial { mod finding; }`, so none of the test_*.rs / location.rs files are compiled or run by any harness. Dozens of adversarial tests are dead (e.g. location_deserialization_rejects_path_traversal never executes). | Wire the orphaned files into a real harness (declare the modules in tests/adversarial.rs or convert to top-level tests/*.rs), or delete the dead copies whose coverage is already in break_it.rs. Assert real values. | status=done | FIXED (finding PREMISE WAS INVERTED - verified by counting #[test]): the test_*.rs files + adversarial/mod.rs were all EMPTY STUBS (0 tests, just a comment line). The REAL tests live in finding.rs (16, was wired), evidence.rs (2) and location.rs (2, incl. the security test location_deserialization_rejects_path_traversal) - evidence.rs and location.rs were the actually-orphaned files. FIXED: harness now declares mod evidence; mod finding; mod location; (all 20 real tests run, +4 incl. the path-traversal security test) and DELETED the 10 dead empty-stub files (9 test_*.rs + mod.rs) as ONE-PLACE cleanup.
src/finding/serialize.rs:185 | silent-fallback | medium | RawFinding::try_from_raw_with_config deserializes raw.references into Arc without sorting or deduplication, unlike other list fields | FIXED: references now sort_unstable()+dedup() before mapping to Arc, matching cve_ids/cwe_ids/matched_values, so a deserialized finding is deterministic and duplicate-free. | status=done
src/finding/ops.rs:102 | perf | medium | Finding::merge_chain converts Arc to String via to_string only to convert back into Arc inside FindingBuilder::build, causing double allocations | FIXED: migrated FindingBuilder's 5 list fields (tags/cve_ids/cwe_ids/references/matched_values) from Vec<String> to Vec<Arc<str>>, and their 7 setter methods (tag/add_tags/cve/add_cves/cwe/reference/matched_value) from `impl Into<String>` to `impl Into<Arc<str>>`. build() now stores them directly instead of `.map(Arc::from)` - saving one alloc per list item on EVERY build. merge_chain passes `Arc::clone` (refcount bump) instead of `.to_string()`, so the whole Arc->String->Arc double-alloc per merged tag/cve/cwe/ref/value is gone. `&str`/`String`/`Arc<str>` callers unchanged (all impl Into<Arc<str>>); fixed 3 test callers that passed `&String` (now `.as_str()`). Full suite: 10 binaries, 263 tests, 0 failed, 0 warnings. sort_unstable/dedup still valid (Arc<str>: Ord). | status=done
src/finding/validate.rs:34 | modernization | low | validate_string_content does not validate or reject Unicode Right-to-Left Override (RLO) characters (u202E), which can be used to spoof file extensions in UI reports | FIXED (bumped in effect to a security fix): validate_string_content now rejects the FULL Trojan Source bidi-control family (U+202A-202E embeddings/overrides + U+2066-2069 isolates + U+061C ALM), not just RLO - rejecting only RLO would leave the sibling overrides as spoofing vectors. New BIDI_CONTROLS const (ONE-PLACE). ALSO fixed a false-confidence test: the existing test_unicode_right_to_left_override embedded a `\0` alongside the RLO so it passed on the null-byte path and never exercised bidi rejection - rewrote it to test RLO in isolation, added test_unicode_bidi_controls_rejected_in_detail_and_target (4 bidi variants rejected + an ordinary-Unicode title still builds). secfinding all targets green. | status=done
src/filter.rs:163 | perf | low | filter performs linear search on finding.tags() for each tag in config.include_tags, resulting in O(N * M) lookup complexity | FIXED: include/exclude tag membership now uses finding.tags().binary_search() (tags are Vec<Arc<str>> sorted by FindingBuilder::build's sort_unstable; Arc<str>: Ord), turning the O(N*M) nested scan into O(M log N). Behavior-identical (Any = intersection non-empty, All = every include tag present); existing filter tests stay green. | status=done
src/finding/builder.rs:341 | capability | low | Finding::new is hardcoded to use FindingConfig::default(), preventing callers from constructing simple findings with custom validation limits | FIXED: added Finding::new_with_config(scanner, target, severity, title, detail, &FindingConfig) as the single owner of the quick-construct logic; new() now delegates to it with FindingConfig::default() (ONE-PLACE, no duplicated validation). Proving test new_with_config_enforces_custom_title_limit (max_title_len=5 rejects a 10-char title while default new() accepts it). | status=done
DONE 2026-07-14 | src/location.rs:83 | capability | low | Location::MAX_PATH_LEN is hardcoded as a constant and cannot be configured via FindingConfig, unlike other validation limits | ASSESSED-ARCHITECTURALLY-AWKWARD (not a clean add): unlike title/scanner/target limits (validated in FindingBuilder::build where FindingConfig IS in scope), validate_location_file runs in TWO config-less contexts - Location's own constructor (location.rs:104) and its serde Deserialize impl (location.rs:202). A Location is a standalone value built/deserialized independently of any Finding, so there is no FindingConfig to consult at those points; MAX_PATH_LEN is a Location-intrinsic safety bound, not a Finding-level policy. Making it config-driven would either couple Location to FindingConfig (breaking the standalone type) or leave the deserialize path un-configurable (a split contract). The hardcoded 16_384 const is the defensible design. | RESOLVED: documented `MAX_PATH_LEN` in `src/location.rs` as a fixed Location-intrinsic safety bound (not a FindingConfig field) because `Location` is constructed and deserialized independently of any `Finding`. `cargo +1.92.0 test -p secfinding --lib` green (CARGO_EXIT=0).
tests/{unit,concurrent,integration,property,regression,adversarial} test-scaffolding cluster | test-gap+dead-code | medium | RECONCILED (single coherent resolution superseding the scattered per-suite/per-stub rows below): every suite dir carried a parallel `test_*.rs` set declared by an unwired `tests/<suite>/mod.rs` that NO top-level target `#[path]`/`mod`-includes, so those files never compiled or ran. Two distinct sub-cases found by counting #[test]: (A) tests/unit/ held a FULLER, divergent-but-STALE extraction (139 fn-ish, 27 real #[test]) orphaned behind an unwired tests/unit/mod.rs while the thin wired tests/unit.rs ran only a subset - the stale set referenced a renamed type (UniversalSeverity->Severity), a 3-field FindingFilter literal (now 12 fields), Vec<String> where fields are Vec<Arc<str>>, and .scanner FIELD access (now an accessor); (B) concurrent/integration/property/regression held mostly EMPTY 1-line comment stubs plus, in concurrent/, one REAL orphaned stress test. | FIXED (salvage-then-remove, coverage-INCREASING): (1) ported all 27 stale-but-real unit tests into the live wired tests/unit/*.rs files updated to the current API (bridge secir-gated x4, evidence x3, filter x7, kind x1, reportable x5 with custom impls trimmed to current trait sigs, severity x2, status x5); (2) salvaged concurrent/test_filter.rs's real 100-thread stress test into the wired secfinding_stress.rs (adapted to current API) as concurrent_filter_reads_are_consistent; (3) DELETED all dead scaffolding crate-wide - every empty 1-line test_*.rs stub + every unwired tests/<suite>/mod.rs + the fully-orphaned tests/regression/ dir (no tests/regression.rs target ever existed). Net: unit target 30->65 tests, whole crate 10 binaries 0 failed 0 warnings green; ONE-PLACE single unit set, one aggregator per suite, no dead routes. adversarial/ was already reconciled earlier (see row 3). | status=done
tests/oom_simulation.rs:17 | test-gap | low | The test simulate_oom_on_very_large_allocation_requests has a tautological assertion that is always true, failing to verify actual behavior | FIXED (Law-6): replaced the `is_err() || is_ok()` tautology. The catch_unwind result is now deliberately discarded (the reservation may succeed under Linux overcommit or fail, so its outcome is not a meaningful invariant); instead, reaching past the catch proves no abort, and a fresh normal Finding::builder(...).build() is asserted Ok to prove the allocator/state stayed consistent after the large-allocation attempt. | status=done
tests/{adversarial,property,concurrent,regression,integration}/test_bridge.rs:1 (+ every sibling test_*.rs stub) | stub | medium | The per-suite test_bridge.rs (and all sibling test_evidence/filter/finding/kind/location/reportable/severity/status stubs) were empty 1-line comment placeholders behind unwired mod.rs files. | FIXED: folded into the test-scaffolding cluster reconciliation above - all empty stub files DELETED as dead code (the real bridge/evidence/etc. behavior is covered by the wired tests/unit/*.rs set, and the secir bridge conversion specifically by the 4 ported secir-gated tests in tests/unit/bridge.rs). status=done